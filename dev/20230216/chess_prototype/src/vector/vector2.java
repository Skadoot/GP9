package vector;

public class vector2
{
    //x coordinate for the board.
    public int x;
    //y coordinate for the board.
    public int y;

    //default constructor with no parameters.
    public vector2()
    {
        this.x = 0;
        this.y = 0;
    }

    //constructor with parameters.
    public vector2(int x, int y)
    {
        this.x = x;
        this.y = y;
    }

    /*
        getter for x, returns x.
        public int get_x()
        {
            return this.x;
        }

        getter for y, returns y.
        public int get_y()
        {
            return this.y;
        }

        setter for x, sets this.x to x parameter.
        public void set_x(int x)
        {
            this.x = x;
        }

        setter for y, sets this.y to y parameter.
        public void set_y(int y)
        {
            this.y = y;
        }
    */
}
